#!/usr/bin/env ruby
#driver program for Dieroll::Roll.string

$:.unshift File.join(File.dirname(__FILE__), '..', 'lib')
require 'dieroll.rb'

string = ARGV.shift

if(ARGV.shift == '-results')
  puts Dieroll::Roll.string(string)
else
  puts Dieroll::Roll.string(string).first
end
