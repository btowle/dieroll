#!/usr/bin/env ruby
#driver program for Dieroll::Roll.string

$:.unshift File.join(File.dirname(__FILE__), '..', 'lib')
require 'dieroll.rb'

string = ARGV.shift

dice = Dieroll::Roll.new(string)

puts dice.roll
puts dice if ARGV.shift == '-results'
